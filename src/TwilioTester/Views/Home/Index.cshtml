@{
    ViewData["Title"] = "TwilioTester";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model IList<TwilioTester.Models.Contact>

<h2>Awesome Text Messaging App!</h2>
<hr />
@Html.ActionLink("View all Messages", "GetMessages")
<br />
@Html.ActionLink("Send a new message", "SendMessage")
<br />
@Html.ActionLink("Add a new Contact", "AddContact")
<br />
@Html.ActionLink("Wikipedia api call page", "Articles", "Home")


@*<h1>Contacts</h1>
@if (Model.Count() == 0)
{

    <p>add contact</p>
}
else
{
@foreach (var contact in Model)
    {
    <li>@contact.name</li>
    <li>@contact.number</li>

    }
}*@

<h1>knockout demo</h1>

<form id="form" data-bind="submit: addItem">
    New Item:
    <input data-bind='value: itemToAdd, valueUpdate: "afterkeydown"'/>
    <button type="submit" data-bind="enable: itemToAdd().length> 0">add</button>
    <p>items:</p>
    <select multiple="multiple" width="50" data-bind="options: items"> </select>
</form>

<span id='123' data-bind="text: name"></span> is working on this project